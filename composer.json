{
      "autoload": {
        "files": [
           "core/function.php" //自动加载的文件,与composer.json 是同一目录下的core
        ],
        "psr-4": {
            "App\\": "app/"
        }
    },
    "config": {
        "preferred-install": "dist"
    }
}
